<Window x:Class="ZenithFiler.SelectFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ZenithFiler"
        xmlns:icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="フォルダを選択"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        MinWidth="400"
        MinHeight="350"
        Width="450"
        Height="450"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontSize="13"
        Closed="Window_Closed"
        FocusManager.FocusedElement="{Binding ElementName=FolderTree}">
    <Window.Resources>
        <local:TreeLineVisibilityConverter x:Key="TreeLineVis"/>
    </Window.Resources>
    <Grid Margin="24,18,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="インデックス検索に追加するフォルダを選択してください。" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <TreeView x:Name="FolderTree" Grid.Row="1"
                  ItemsSource="{Binding Drives}"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource BorderBrush}"
                  Padding="5"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <TreeView.Resources>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{StaticResource SelectionColor}"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="{StaticResource AccentColor}"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="{StaticResource InactiveSelectionColor}"/>
                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="{StaticResource PrimaryTextColor}"/>
            </TreeView.Resources>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    <Setter Property="Padding" Value="1,1"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TreeViewItem">
                                <Grid SnapsToDevicePixels="True">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <!-- ツリー接続線: 非末尾子の縦線 -->
                                    <Rectangle x:Name="TreeLineVertFull"
                                               Grid.Row="0"
                                               Width="1" HorizontalAlignment="Left"
                                               Fill="{DynamicResource TreeLineBrush}"
                                               Margin="-7,0,0,0"
                                               SnapsToDevicePixels="True"
                                               Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource TreeLineVis}, ConverterParameter=vert-full}"/>
                                    <!-- ツリー接続線: 末尾子の縦線（L 字角まで） -->
                                    <Rectangle x:Name="TreeLineVertLast"
                                               Grid.Row="0"
                                               Width="1" HorizontalAlignment="Left"
                                               VerticalAlignment="Top"
                                               Fill="{DynamicResource TreeLineBrush}"
                                               Margin="-7,0,0,0"
                                               SnapsToDevicePixels="True"
                                               Height="{Binding ActualHeight, ElementName=DlgFullRowBg, Converter={x:Static local:HalfValueConverter.Instance}}"
                                               Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource TreeLineVis}, ConverterParameter=vert-last}"/>
                                    <!-- ツリー接続線: 水平コネクタ -->
                                    <Rectangle x:Name="TreeLineHorz"
                                               Grid.Row="0"
                                               Width="27" Height="1"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Center"
                                               Fill="{DynamicResource TreeLineBrush}"
                                               Margin="-7,0,0,0"
                                               Panel.ZIndex="1"
                                               IsHitTestVisible="False"
                                               SnapsToDevicePixels="True"
                                               Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource TreeLineVis}, ConverterParameter=horz}"/>
                                    <!-- ツリー接続線: 子要素領域の継続縦線 -->
                                    <Rectangle x:Name="TreeLineContinuation"
                                               Grid.Row="1"
                                               Width="1" HorizontalAlignment="Left"
                                               Fill="{DynamicResource TreeLineBrush}"
                                               Margin="-7,0,0,0"
                                               SnapsToDevicePixels="True"
                                               Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource TreeLineVis}, ConverterParameter=vert-full}"/>
                                    <Border x:Name="DlgFullRowBg"
                                            Grid.Row="0"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="3"
                                            Margin="0,0,4,0"
                                            SnapsToDevicePixels="True">
                                        <Grid Margin="{TemplateBinding Padding}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" MinWidth="19"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <ToggleButton x:Name="Expander"
                                                          IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                          ClickMode="Press"
                                                          Focusable="False"
                                                          Width="16" Height="16"
                                                          Margin="0,0,2,0">
                                                <ToggleButton.Template>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Border Background="Transparent" Width="16" Height="16">
                                                            <icon:PackIconLucide x:Name="ExpanderIcon"
                                                                                 Kind="ChevronRight"
                                                                                 Width="10" Height="10"
                                                                                 VerticalAlignment="Center"
                                                                                 HorizontalAlignment="Center"
                                                                                 Foreground="{DynamicResource SubTextBrush}"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="ExpanderIcon" Property="Kind" Value="ChevronDown"/>
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ExpanderIcon" Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </ToggleButton.Template>
                                            </ToggleButton>
                                            <ContentPresenter x:Name="PART_Header"
                                                              Grid.Column="1"
                                                              ContentSource="Header"
                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Grid>
                                    </Border>
                                    <ItemsPresenter x:Name="ItemsHost"
                                                    Grid.Row="1"
                                                    Margin="19,0,0,0"
                                                    Visibility="Collapsed"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsExpanded" Value="True">
                                        <Setter TargetName="ItemsHost" Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                    <Trigger Property="HasItems" Value="False">
                                        <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="DlgFullRowBg" Property="Background" Value="{DynamicResource ListHoverBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="DlgFullRowBg" Property="Background" Value="{DynamicResource SelectionBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <Grid Margin="0,0,4,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button Content="OK"
                    IsDefault="True"
                    Click="OkButton_Click"
                    MinWidth="96"
                    MinHeight="30"
                    Padding="14,4"
                    Margin="0,0,12,0"/>
            <Button Content="キャンセル"
                    IsCancel="True"
                    MinWidth="96"
                    MinHeight="30"
                    Padding="14,4"/>
        </StackPanel>
    </Grid>
</Window>
