<Window x:Class="ZenithFiler.BackupListDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:icon="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Lucide"
        Title="設定バックアップ一覧" Width="740" Height="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FocusManager.FocusedElement="{Binding ElementName=BackupListView}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="BackupListView" Grid.Row="0"
                  Margin="10,10,10,4"
                  MouseDoubleClick="BackupListView_MouseDoubleClick"
                  SelectionMode="Single">
            <ListView.ContextMenu>
                <ContextMenu ItemContainerStyle="{StaticResource ContextMenuItemStyle}">
                    <MenuItem Header="ロック" Click="LockMenuItem_Click"/>
                    <MenuItem Header="ロック解除" Click="UnlockMenuItem_Click"/>
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="日時" Width="160">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Timestamp, StringFormat=yyyy/MM/dd HH:mm:ss}" FontSize="12"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="ロック" Width="52">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <icon:PackIconLucide Width="14" Height="14" Margin="4,0">
                                    <icon:PackIconLucide.Style>
                                        <Style TargetType="icon:PackIconLucide">
                                            <Setter Property="Kind" Value="LockOpen"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLocked}" Value="True">
                                                    <Setter Property="Kind" Value="Lock"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </icon:PackIconLucide.Style>
                                </icon:PackIconLucide>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="変更概要" Width="460">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Summary}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="10,4,10,10">
            <Button Content="閉じる" Click="CloseButton_Click" Width="90" Padding="0,6"
                    IsTabStop="False"/>
        </StackPanel>
    </Grid>
</Window>
